swarm:
  logging:
    root-logger:
      level: INFO
  undertow:
    servlet-containers:
      default:
        websockets-setting:
          per-message-deflate: true
  messaging-activemq:
    servers:
      default:
        security-enabled: false
        jms-queues:
          echo: {}